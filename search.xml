<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>位运算题目及技巧</title>
    <url>/2025/12/19/leetcode/bit/bit/</url>
    <content><![CDATA[<h2 id="异或运算"><a href="#异或运算" class="headerlink" title="异或运算"></a>异或运算</h2><h3 id="计算海明距离"><a href="#计算海明距离" class="headerlink" title="计算海明距离"></a>计算海明距离</h3><p><strong>题目：</strong></p>
<p>两个整数之间的<strong>汉明距离</strong>指的是这两个数字对应二进制位不同的位置的数目。</p>
<p>给你两个整数 x 和 y，计算并返回它们之间的汉明距离。</p>
<p><strong>思路：</strong></p>
<p>可以通过先计算两个数的异或，然后再计算它们 1 的数目。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span>:</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">hammingDistance</span>(<span class="params">self, x: <span class="built_in">int</span>, y: <span class="built_in">int</span></span>) -&gt; <span class="built_in">int</span>:</span><br><span class="line">        <span class="keyword">return</span> (x ^ y).bit_count()</span><br></pre></td></tr></table></figure>

<h3 id="数字的补数"><a href="#数字的补数" class="headerlink" title="数字的补数"></a>数字的补数</h3><p><strong>题目：</strong></p>
<p>对整数的二进制表示取反（0 变 1 ，1 变 0）后，再转换为十进制表示，可以得到这个整数的补数。</p>
<p>例如，整数 5 的二进制表示是 “101” ，取反后得到 “010” ，再转回十进制表示得到补数 2 。<br>给你一个整数 num ，输出它的补数。</p>
<p><strong>思路：</strong></p>
<p>可以先计算异或，但是为了防止前导 0 的问题，可以用 mask 进行去除。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span>:</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">findComplement</span>(<span class="params">self, num: <span class="built_in">int</span></span>) -&gt; <span class="built_in">int</span>:</span><br><span class="line">        <span class="keyword">return</span> num ^ ((<span class="number">1</span> &lt;&lt; num.bit_length()) - <span class="number">1</span>)</span><br></pre></td></tr></table></figure>

<h3 id="形成两个异或相等数组的三元组数目"><a href="#形成两个异或相等数组的三元组数目" class="headerlink" title="形成两个异或相等数组的三元组数目"></a>形成两个异或相等数组的三元组数目</h3><p><strong>题目：</strong></p>
<p>给你一个整数数组 arr 。</p>
<p>现需要从数组中取三个下标 i、j 和 k ，其中 (0 &lt;&#x3D; i &lt; j &lt;&#x3D; k &lt; arr.length) 。</p>
<p>a 和 b 定义如下：</p>
<p>a &#x3D; arr[i] ^ arr[i + 1] ^ … ^ arr[j - 1]<br>b &#x3D; arr[j] ^ arr[j + 1] ^ … ^ arr[k]<br>注意：^ 表示 按位异或 操作。</p>
<p>请返回能够令 a &#x3D;&#x3D; b 成立的三元组 (i, j , k) 的数目。</p>
<p><strong>思路：</strong></p>
<p>注意到，S[i] &#x3D;&#x3D; S[k]时，j 在任何位置都成立。</p>
<p>因此遍历时，可以记录之前的元素的异或和，并且记录该异或和值所对应的下标总和。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span>:</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">countTriplets</span>(<span class="params">self, arr: <span class="type">List</span>[<span class="built_in">int</span>]</span>) -&gt; <span class="built_in">int</span>:</span><br><span class="line">        cnt, total = Counter(), Counter()</span><br><span class="line">        ans = s = <span class="number">0</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> k, val <span class="keyword">in</span> <span class="built_in">enumerate</span>(arr):</span><br><span class="line">            <span class="keyword">if</span> (t := s ^ val) <span class="keyword">in</span> cnt:</span><br><span class="line">                ans += cnt[t] * k - total[t]</span><br><span class="line">            cnt[s] += <span class="number">1</span></span><br><span class="line">            total[s] += k</span><br><span class="line">            s = t</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> ans</span><br></pre></td></tr></table></figure>

<h2 id="位取反"><a href="#位取反" class="headerlink" title="位取反"></a>位取反</h2><h3 id="将找到的数值乘以-2"><a href="#将找到的数值乘以-2" class="headerlink" title="将找到的数值乘以 2"></a>将找到的数值乘以 2</h3><p><strong>题目：</strong></p>
<p>给你一个整数数组 nums ，另给你一个整数 original ，这是需要在 nums 中搜索的第一个数字。</p>
<p>接下来，你需要按下述步骤操作：</p>
<ol>
<li>如果在 nums 中找到 original ，将 original 乘以 2 ，得到新 original（即，令 original &#x3D; 2 * original）。</li>
<li>否则，停止这一过程。</li>
<li>只要能在数组中找到新 original ，就对新 original 继续 重复 这一过程。<br>返回 original 的 最终 值。</li>
</ol>
<p><strong>思路：</strong></p>
<p>以 original 为单位，只有 2 的幂才符合需求。</p>
<p>找到第一个不在其中的 2 的幂。</p>
<p>找到第一个不在其中的 2 的幂，可以转化为找到最低位的 0。我们可以通过取反操作，变为寻找最低的 1。</p>
<p>那么最终就可以用<code>mask &amp; -mask</code>来寻找。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span>:</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">findFinalValue</span>(<span class="params">self, nums: <span class="type">List</span>[<span class="built_in">int</span>], original: <span class="built_in">int</span></span>) -&gt; <span class="built_in">int</span>:</span><br><span class="line">        mask = <span class="number">0</span></span><br><span class="line">        <span class="keyword">for</span> num <span class="keyword">in</span> nums:</span><br><span class="line">            <span class="keyword">if</span> num % original == <span class="number">0</span>:</span><br><span class="line">                d = num // original</span><br><span class="line">                <span class="keyword">if</span> d &amp; (d - <span class="number">1</span>) == <span class="number">0</span>:</span><br><span class="line">                    mask |= d</span><br><span class="line"></span><br><span class="line">        mask = ~mask</span><br><span class="line">        lowbit = mask &amp; -mask</span><br><span class="line">        <span class="keyword">return</span> original * lowbit</span><br></pre></td></tr></table></figure>

<h2 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h2><h3 id="二进制间距"><a href="#二进制间距" class="headerlink" title="二进制间距"></a>二进制间距</h3><p><strong>题目：</strong></p>
<p>给定一个正整数 n，找到并返回 n 的二进制表示中两个 相邻 1 之间的 最长距离 。如果不存在两个相邻的 1，返回 0 。</p>
<p>如果只有 0 将两个 1 分隔开（可能不存在 0 ），则认为这两个 1 彼此 相邻 。两个 1 之间的距离是它们的二进制表示中位置的绝对差。例如，”1001” 中的两个 1 的距离为 3 。</p>
<p><strong>思路：</strong></p>
<p>进行模拟即可。</p>
<p>这个模拟可以作为一个查找相邻距离的模板。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span>:</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">binaryGap</span>(<span class="params">self, n: <span class="built_in">int</span></span>) -&gt; <span class="built_in">int</span>:</span><br><span class="line">        last = -<span class="number">1</span></span><br><span class="line">        ans = <span class="number">0</span></span><br><span class="line">        cur = <span class="number">0</span></span><br><span class="line">        <span class="keyword">while</span> n:</span><br><span class="line">            <span class="keyword">if</span> n &amp; <span class="number">1</span> == <span class="number">1</span>:</span><br><span class="line">                <span class="keyword">if</span> last != -<span class="number">1</span>:</span><br><span class="line">                    ans = <span class="built_in">max</span>(ans, cur - last)</span><br><span class="line">                last = cur</span><br><span class="line">            n &gt;&gt;= <span class="number">1</span></span><br><span class="line">            cur += <span class="number">1</span></span><br><span class="line">        <span class="keyword">return</span> ans</span><br></pre></td></tr></table></figure>

<h3 id="插入数字"><a href="#插入数字" class="headerlink" title="插入数字"></a>插入数字</h3><p><strong>题目：</strong></p>
<p>给定两个整型数字 N 与 M，以及表示比特位置的 i 与 j（i &lt;&#x3D; j，且从 0 位开始计算）。</p>
<p>编写一种方法，使 M 对应的二进制数字插入 N 对应的二进制数字的第 i ~ j 位区域，不足之处用 0 补齐。具体插入过程如图所示。</p>
<p><strong>思路：</strong></p>
<p>先创建一个 mask，然后再插入。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span>:</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">insertBits</span>(<span class="params">self, N: <span class="built_in">int</span>, M: <span class="built_in">int</span>, i: <span class="built_in">int</span>, j: <span class="built_in">int</span></span>) -&gt; <span class="built_in">int</span>:</span><br><span class="line">        <span class="comment"># 先把N从i到j位置零</span></span><br><span class="line">        mask = (<span class="number">1</span> &lt;&lt; (j - i + <span class="number">1</span>)) - <span class="number">1</span></span><br><span class="line">        mask = mask &lt;&lt; i</span><br><span class="line">        mask = ~mask</span><br><span class="line">        N &amp;= mask</span><br><span class="line">        <span class="keyword">return</span> N | (M &lt;&lt; i)</span><br></pre></td></tr></table></figure>

<h3 id="比特位计数"><a href="#比特位计数" class="headerlink" title="比特位计数"></a>比特位计数</h3><p><strong>题目：</strong></p>
<p>给你一个整数 n ，对于 0 &lt;&#x3D; i &lt;&#x3D; n 中的每个 i ，计算其二进制表示中 1 的个数 ，返回一个长度为 n + 1 的数组 ans 作为答案。</p>
<p><strong>思路：</strong></p>
<p>采用动态规划的方法，去除最高位的 bit 后，剩余的部分应该已经得出，直接加 1 即可。</p>
<p>若为 2 的幂，则更新最高位的 bit。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span>:</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">countBits</span>(<span class="params">self, n: <span class="built_in">int</span></span>) -&gt; <span class="type">List</span>[<span class="built_in">int</span>]:</span><br><span class="line">        ans = [<span class="number">0</span>]</span><br><span class="line">        highBit = <span class="number">0</span></span><br><span class="line">        <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">1</span>, n + <span class="number">1</span>):</span><br><span class="line">            <span class="keyword">if</span> i &amp; (i - <span class="number">1</span>) == <span class="number">0</span>:</span><br><span class="line">                highBit = i</span><br><span class="line">            ans.append(ans[i - highBit] + <span class="number">1</span>)</span><br><span class="line">        <span class="keyword">return</span> ans</span><br></pre></td></tr></table></figure>

<h3 id="颠倒二进制位"><a href="#颠倒二进制位" class="headerlink" title="颠倒二进制位"></a>颠倒二进制位</h3><p><strong>题目：</strong></p>
<p>颠倒给定的 32 位有符号整数的二进制位。</p>
<p><strong>思路：</strong></p>
<p>通过位运算，进行分治。</p>
<p>先分割两块，进行交换；然后每块内部再进行交换。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span>:</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">reverseBits</span>(<span class="params">self, n: <span class="built_in">int</span></span>) -&gt; <span class="built_in">int</span>:</span><br><span class="line">        <span class="comment"># 交换 16 位</span></span><br><span class="line">        n = ((n &gt;&gt; <span class="number">16</span>) | (n &lt;&lt; <span class="number">16</span>)) &amp; <span class="number">0xFFFFFFFF</span></span><br><span class="line">        <span class="comment"># 交换每个 8 位块</span></span><br><span class="line">        n = (((n &amp; <span class="number">0xFF00FF00</span>) &gt;&gt; <span class="number">8</span>) | ((n &amp; <span class="number">0x00FF00FF</span>) &lt;&lt; <span class="number">8</span>)) &amp; <span class="number">0xFFFFFFFF</span></span><br><span class="line">        <span class="comment"># 交换每个 4 位块</span></span><br><span class="line">        n = (((n &amp; <span class="number">0xF0F0F0F0</span>) &gt;&gt; <span class="number">4</span>) | ((n &amp; <span class="number">0x0F0F0F0F</span>) &lt;&lt; <span class="number">4</span>)) &amp; <span class="number">0xFFFFFFFF</span></span><br><span class="line">        <span class="comment"># 交换每个 2 位块</span></span><br><span class="line">        n = (((n &amp; <span class="number">0xCCCCCCCC</span>) &gt;&gt; <span class="number">2</span>) | ((n &amp; <span class="number">0x33333333</span>) &lt;&lt; <span class="number">2</span>)) &amp; <span class="number">0xFFFFFFFF</span></span><br><span class="line">        <span class="comment"># 交换相邻位</span></span><br><span class="line">        n = (((n &amp; <span class="number">0xAAAAAAAA</span>) &gt;&gt; <span class="number">1</span>) | ((n &amp; <span class="number">0x55555555</span>) &lt;&lt; <span class="number">1</span>)) &amp; <span class="number">0xFFFFFFFF</span></span><br><span class="line">        <span class="keyword">return</span> n</span><br></pre></td></tr></table></figure>

<h3 id="最少反转次数得到翻转二进制字符串"><a href="#最少反转次数得到翻转二进制字符串" class="headerlink" title="最少反转次数得到翻转二进制字符串"></a>最少反转次数得到翻转二进制字符串</h3><p><strong>题目：</strong></p>
<p>给你一个 正 整数 n。</p>
<p>令 s 为 n 的 二进制表示（不含前导零）。</p>
<p>二进制字符串 s 的 反转 是指将 s 中的字符按相反顺序排列得到的字符串。</p>
<p>你可以翻转 s 中的任意一位（将 0 → 1 或 1 → 0）。每次翻转 仅 影响一位。</p>
<p>请返回使 s 等于其原始形式的反转所需的 最少 翻转次数。</p>
<p><strong>思路：</strong></p>
<p>可以先转化为字符串，然后比较 reverse 和原字符串的差别。</p>
<p>python 可以调用<code>bin()</code>函数，获取数字的二进制表示，二进制表示以”0b”开始，因此需要去除。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span>:</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">minimumFlips</span>(<span class="params">self, n: <span class="built_in">int</span></span>) -&gt; <span class="built_in">int</span>:</span><br><span class="line">        s = <span class="built_in">bin</span>(n)[<span class="number">2</span>:]</span><br><span class="line">        ans = <span class="number">0</span></span><br><span class="line">        l = <span class="built_in">len</span>(s)</span><br><span class="line">        <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(l):</span><br><span class="line">            <span class="keyword">if</span> s[i] != s[l - i - <span class="number">1</span>]:</span><br><span class="line">                ans += <span class="number">1</span></span><br><span class="line">        <span class="keyword">return</span> ans</span><br></pre></td></tr></table></figure>

<h3 id="生成不含相邻-0-的子字符串"><a href="#生成不含相邻-0-的子字符串" class="headerlink" title="生成不含相邻 0 的子字符串"></a>生成不含相邻 0 的子字符串</h3><!-- TODO：换到回溯里 -->

<p><strong>题目：</strong></p>
<p>给你一个正整数 n。</p>
<p>如果一个二进制字符串 x 的所有长度为 2 的子字符串中包含 至少 一个 “1”，则称 x 是一个 有效 字符串。</p>
<p>返回所有长度为 n 的 有效 字符串，可以以任意顺序排列。</p>
<p><strong>思路：</strong></p>
<p>因为返回了所有的子字符串（需要得到每一个解），因此可以考虑回溯法。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span>:</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">validStrings</span>(<span class="params">self, n: <span class="built_in">int</span></span>) -&gt; <span class="type">List</span>[<span class="built_in">str</span>]:</span><br><span class="line">        res = []</span><br><span class="line">        <span class="keyword">def</span> <span class="title function_">dfs</span>(<span class="params">arr: <span class="type">List</span>[<span class="built_in">str</span>]</span>) -&gt; <span class="literal">None</span>:</span><br><span class="line">            <span class="keyword">if</span> <span class="built_in">len</span>(arr) == n:</span><br><span class="line">                res.append(<span class="string">&#x27;&#x27;</span>.join(arr))</span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                <span class="keyword">if</span> arr == [] <span class="keyword">or</span> arr[-<span class="number">1</span>] == <span class="string">&#x27;1&#x27;</span>:</span><br><span class="line">                    arr.append(<span class="string">&#x27;0&#x27;</span>)</span><br><span class="line">                    dfs(arr)</span><br><span class="line">                    arr.pop()</span><br><span class="line">                arr.append(<span class="string">&#x27;1&#x27;</span>)</span><br><span class="line">                dfs(arr)</span><br><span class="line">                arr.pop()</span><br><span class="line">        dfs([])</span><br><span class="line">        <span class="keyword">return</span> res</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>算法</category>
        <category>位运算</category>
      </categories>
  </entry>
  <entry>
    <title>字典树题目及技巧</title>
    <url>/2025/12/26/leetcode/trie/trie/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>算法</category>
        <category>字典树</category>
      </categories>
  </entry>
  <entry>
    <title>图算法</title>
    <url>/2025/12/19/leetcode/graph/graph/</url>
    <content><![CDATA[<h2 id="DFS"><a href="#DFS" class="headerlink" title="DFS"></a>DFS</h2><h3 id="所有的可能路径"><a href="#所有的可能路径" class="headerlink" title="所有的可能路径"></a>所有的可能路径</h3><p><strong>题目：</strong></p>
<p>给你一个有 n 个节点的 有向无环图（DAG），请你找出从节点 0 到节点 n-1 的所有路径并输出（不要求按特定顺序）</p>
<p><code>graph[i]</code> 是一个从节点 i 可以访问的所有节点的列表（即从节点 i 到节点 <code>graph[i][j]</code>存在一条有向边）。</p>
<p><strong>思路：</strong></p>
<p>注意到是有向无环图，因此可以直接 dfs，不用管是否会因为返回而成环。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span>:</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">allPathsSourceTarget</span>(<span class="params">self, graph: <span class="type">List</span>[<span class="type">List</span>[<span class="built_in">int</span>]]</span>) -&gt; <span class="type">List</span>[<span class="type">List</span>[<span class="built_in">int</span>]]:</span><br><span class="line">        ans = []</span><br><span class="line">        cur_path = []</span><br><span class="line">        n = <span class="built_in">len</span>(graph)</span><br><span class="line"></span><br><span class="line">        <span class="keyword">def</span> <span class="title function_">dfs</span>(<span class="params">cur</span>):</span><br><span class="line">            cur_path.append(cur)</span><br><span class="line">            <span class="keyword">if</span> cur == n - <span class="number">1</span>:</span><br><span class="line">                ans.append(cur_path[:])</span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                <span class="keyword">for</span> nxt <span class="keyword">in</span> graph[cur]:</span><br><span class="line">                    dfs(nxt)</span><br><span class="line">            cur_path.pop()</span><br><span class="line"></span><br><span class="line">        dfs(<span class="number">0</span>)</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> ans</span><br></pre></td></tr></table></figure>

<h2 id="图建模"><a href="#图建模" class="headerlink" title="图建模"></a>图建模</h2><h3 id="拆分合并数组"><a href="#拆分合并数组" class="headerlink" title="拆分合并数组"></a>拆分合并数组</h3><p><strong>题目：</strong></p>
<p>给你两个长度为 <code>n</code> 的整数数组 <code>nums1</code> 和 <code>nums2</code>。你可以对 <code>nums1</code> 执行任意次下述的 拆分合并操作：</p>
<ol>
<li>选择一个子数组 <code>nums1[L..R]</code>。</li>
<li>移除该子数组，留下前缀 <code>nums1[0..L-1]</code>（如果 <code>L=0</code> 则为空）和后缀 <code>nums1[R+1..n-1]</code>（如果 <code>R=n-1</code> 则为空）。</li>
<li>将移除的子数组（按原顺序）重新插入到剩余数组的 任意 位置（即，在任意两个元素之间、最开始或最后面）。</li>
</ol>
<p>返回将 <code>nums1</code> 转换为 <code>nums2</code> 所需的 最少拆分合并操作 次数。</p>
<p><strong>思路：</strong></p>
<p>观察到 nums 的长度很小，因此采用建图的方法，枚举所有可转移的状态，进行 BFS 。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span>:</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">minSplitMerge</span>(<span class="params">self, nums1: <span class="type">List</span>[<span class="built_in">int</span>], nums2: <span class="type">List</span>[<span class="built_in">int</span>]</span>) -&gt; <span class="built_in">int</span>:</span><br><span class="line">        <span class="comment"># length的数目极少，因此可以通过枚举所有的转移，并且记录状态</span></span><br><span class="line">        n = <span class="built_in">len</span>(nums1)</span><br><span class="line">        vis = &#123;<span class="built_in">tuple</span>(nums1)&#125;</span><br><span class="line">        q = deque()</span><br><span class="line">        q.append(nums1)</span><br><span class="line">        ans = <span class="number">0</span></span><br><span class="line">        <span class="keyword">while</span> q:</span><br><span class="line">            size = <span class="built_in">len</span>(q)</span><br><span class="line">            <span class="keyword">for</span> _ <span class="keyword">in</span> <span class="built_in">range</span>(size):</span><br><span class="line">                curNums = q.popleft()</span><br><span class="line">                <span class="keyword">if</span> curNums == nums2:</span><br><span class="line">                    <span class="keyword">return</span> ans</span><br><span class="line">                <span class="keyword">for</span> l <span class="keyword">in</span> <span class="built_in">range</span>(n):</span><br><span class="line">                    <span class="keyword">for</span> r <span class="keyword">in</span> <span class="built_in">range</span>(l + <span class="number">1</span>, n + <span class="number">1</span>):</span><br><span class="line">                        b = curNums[:l] + curNums[r:]</span><br><span class="line">                        sub = curNums[l:r]</span><br><span class="line">                        <span class="comment"># 进行拼接</span></span><br><span class="line">                        <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="built_in">len</span>(b) + <span class="number">1</span>):</span><br><span class="line">                            nextNums = b[:i] + sub + b[i:]</span><br><span class="line">                            t = <span class="built_in">tuple</span>(nextNums)</span><br><span class="line">                            <span class="keyword">if</span> t <span class="keyword">not</span> <span class="keyword">in</span> vis:</span><br><span class="line">                                vis.add(t)</span><br><span class="line">                                q.append(nextNums)</span><br><span class="line">            ans += <span class="number">1</span></span><br><span class="line">        <span class="keyword">return</span> -<span class="number">1</span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>算法</category>
        <category>图算法</category>
      </categories>
  </entry>
</search>
